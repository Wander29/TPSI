var ogg = {
    nomep1: 'cazoz',
    nomep2: 'cazez'
};
do {
    var matto = Math.round(Math.random() * 2);
} while (matto === 0);
    

//var appo = "nomep" + matto;
if (matto === 1) {
    document.write("beh come posso dire " + ogg.nomep1) ;
} ;
 if (matto === 2) {
    document.write("beh come posso dire " + ogg.nomep2) ;
} ;